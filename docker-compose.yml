services:
  backend:
    build:
      context: ./backend
    container_name: todo-backend
    ports:
      - "8000:8000"

  frontend:
    build:
      context: ./frontend
    container_name: todo-frontend
    ports:
      - "8080:80"
    depends_on:
      - backend
